using Unity.Netcode;
using UnityEngine;
using static MonScript;

public class ParentCommunicator : NetworkBehaviour
{
    //// Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}
    //public void sendRequest(MonScript mon)
    //{
    //    SubmitMoveServerRpc(mon);
    //}
    //[ServerRpc]
    //void SubmitMoveServerRpc(MonScript mon)
    //{
    //    Debug.Log("TESTMove submitted");

    //    // Register player if not already assigned
    //    ServerScript.Instance.RegisterPlayer(mon);
    //    ServerScript.Instance.RegisterMove(mon);
    //}
}
